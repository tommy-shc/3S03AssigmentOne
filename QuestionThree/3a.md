Create a stub implementation of the CatanAgent interface. A stub is a minimal hardcoded implementation that always returns valid fixed values, allowing the core game engine to finction and be tested without needing any real agent logic. This decouples the agent behaviour from the core game since the core module just sees CatanAgent and doesn't care whether it's a real AI or a stub.